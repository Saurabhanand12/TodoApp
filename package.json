{
  "name": "todo-app-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.0.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && New-Item -ItemType Directory -Force -Path dist && Copy-Item -Path client\\dist\\* -Destination dist\\ -Recurse -Force"
  }
}
